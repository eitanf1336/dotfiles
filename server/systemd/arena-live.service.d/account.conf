# The arena bills Eitan's own Claude login (eitanf1336), not the server's default
# (dad's). ~/.claude-arena holds only that login plus a link to the bots' sessions.
[Service]
Environment=CLAUDE_CONFIG_DIR=%h/.claude-arena
